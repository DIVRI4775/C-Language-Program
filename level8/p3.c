#include<stdio.h>
int main()
{
	int n,i,r;
    printf("Enter the number who's factorial to be calculated\n");
    scanf("%d",&n);
    r= fac(n);
    printf("the factorial is %d",r);
    return 0;
}
int fac(int nn)
{
	
	return fac(nn);
}
    
